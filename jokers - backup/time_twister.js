
function time_twister_effect(points, mult, hand, gameState, handInfo) {
    // This joker's effect is handled in the main game loop.
    return { points: points, mult: mult, message: "Time twisted!" };
}
